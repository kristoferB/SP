'use strict';

describe('Service: itemSvc', function () {

  // load the service's module
  beforeEach(module('spGuiApp'));

  // instantiate service
  var itemSvc;
  beforeEach(inject(function (_itemSvc_) {
    itemSvc = _itemSvc_;
  }));

});
