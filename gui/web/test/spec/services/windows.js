'use strict';

describe('Service: Windows', function () {

  // load the service's module
  beforeEach(module('spGuiApp'));

  // instantiate service
  var Windows;
  beforeEach(inject(function (_Windows_) {
    Windows = _Windows_;
  }));


});
