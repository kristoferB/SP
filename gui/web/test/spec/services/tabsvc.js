'use strict';

describe('Service: tabSvc', function () {

  // load the service's module
  beforeEach(module('spGuiApp'));

  // instantiate service
  var tabSvc;
  beforeEach(inject(function (_tabSvc_) {
    tabSvc = _tabSvc_;
  }));


});
