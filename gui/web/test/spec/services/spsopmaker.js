'use strict';

describe('Service: spSopMaker', function () {

  // load the service's module
  beforeEach(module('spGuiApp'));

  // instantiate service
  var spSopMaker;
  beforeEach(inject(function (_spSopMaker_) {
    spSopMaker = _spSopMaker_;
  }));

  

});
