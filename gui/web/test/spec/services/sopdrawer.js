'use strict';

describe('Service: sopDrawer', function () {

  // load the service's module
  beforeEach(module('spGuiApp'));

  // instantiate service
  var sopDrawer;
  beforeEach(inject(function (_sopDrawer_) {
    sopDrawer = _sopDrawer_;
  }));

});
