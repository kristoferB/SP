'use strict';

describe('Directive: restTest', function () {

  // load the directive's module
  beforeEach(module('spGuiApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  
});
